# Change this in production
AUTH_SECRET=replace-with-long-random-string

# Optional: override default admin username and password on first run.
# If data/db.json already exists, these won't reset automatically.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
